name: Deployment

on:
  workflow_dispatch:
jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: stagint
    steps:
      - name: deploy
        run: echo 'deploy...'
  lint:
    runs-on: ubuntu-latest
    environment: stagint
    steps:
      - name: lint
        run: echo 'lint...'
